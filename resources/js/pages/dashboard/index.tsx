import AppLayout from "@/components/ui/core/layout/app/app-layout";

export default function Pages({ ...props}) {
    console.log(props)
    return (
        <AppLayout >

         
            
        </AppLayout>
    );
}
